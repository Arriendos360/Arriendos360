{
  "name": "arriendos360-shared",
  "version": "0.0.0",
  "private": true,
  "description": "Utilidades compartidas entre servicios: verificacion local del JWT, forma estandar de error, autenticacion entre servicios, bus de eventos y calendario.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepare": "npm run build",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^30.4.2",
    "ts-jest": "^29.2.5"
  }
}
